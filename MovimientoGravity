package movimientos;

import excepciones.MovimientoInvalido;
import logica.Ficha;
import logica.Tablero;

public class MovimientoGravity extends Movimiento{
	
	public MovimientoGravity(int fila, int columna, Ficha color){
		super(fila, columna, color);
	}

	public void ejecutaMovimiento(Tablero tab) throws MovimientoInvalido{
		//observamos columnas
		if(this.columna > (tab.getNumColumnas()/2)){
			//chequear a que distancia esta la ultima ficha a la izquierda
		}
		else{
			//chequear a que distancia esta la ultima ficha a la derecha
		}
		//observamos filas
		if(this.fila > (tab.getNumFilas()/2)){
			//chequar a que distancia esta la ultima ficha hacia abajo, esto es la funcion del complica
		}
		else{
			//chequear a que distancia esta la ultima ficha hacia arriba
		}
	}

	@Override
	public void undo(Tablero t) {
		// TODO Auto-generated method stub
		
	}
}
